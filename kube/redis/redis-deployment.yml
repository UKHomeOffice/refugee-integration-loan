---
apiVersion: apps/v1
kind: Deployment
metadata:
  {{ if eq .KUBE_NAMESPACE .BRANCH_ENV }}
  name: redis-{{ .DRONE_SOURCE_BRANCH }}
  {{ else }}
  name: redis
  {{ end }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{ if eq .KUBE_NAMESPACE .BRANCH_ENV }}
      app: redis-{{ .DRONE_SOURCE_BRANCH }}
      {{ else }}
      app: redis
      {{ end }}
  template:
    metadata:
      labels:
        {{ if eq .KUBE_NAMESPACE .BRANCH_ENV }}
        name: redis-{{ .DRONE_SOURCE_BRANCH }}
        service: redis-{{ .DRONE_SOURCE_BRANCH }}
        app: redis-{{ .DRONE_SOURCE_BRANCH }}
        {{ else }}
        name: redis
        service: redis
        app: redis
        {{ end }}
    spec:
      securityContext:
        fsGroup: 994
        fsGroupChangePolicy: OnRootMismatch
      containers:
        - name: redis
          image: quay.io/ukhomeofficedigital/hof-docker-redis:8.4.2@sha256:e0ae9e895f3f5b5b1b42fd311f07c4ec4b6a09b760ed129593cad8bc3c681202
          ports:
            - containerPort: 6379
          volumeMounts:
            - mountPath: /var/lib/redis
              name: data
          securityContext:
            runAsNonRoot: true
          resources:
            requests:
              cpu: "20m"
              memory: "100Mi"
            limits:
              cpu: "100m"
              memory: "200Mi"
      volumes:
        {{ if and (eq .REDIS_PERSISTENCE_ENABLED "true") (ne .REDIS_PERSISTENCE_EXISTING_CLAIM "") }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ .REDIS_PERSISTENCE_EXISTING_CLAIM }}
        {{ else if eq .REDIS_PERSISTENCE_ENABLED "true" }}
        - name: data
          persistentVolumeClaim:
            {{ if eq .KUBE_NAMESPACE .BRANCH_ENV }}
            claimName: redis-pvc-{{ .DRONE_SOURCE_BRANCH }}
            {{ else }}
            claimName: redis-pvc
            {{ end }}
        {{ else }}
        - name: data
          emptyDir: {}
        {{ end }}
