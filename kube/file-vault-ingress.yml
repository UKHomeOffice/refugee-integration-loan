apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: file-vault-ingress
  annotations:
    ingress.kubernetes.io/secure-backends: "true"
    ingress.kubernetes.io/backend-protocol: "HTTPS"
    kubernetes.io/ingress.class: "nginx-external"
    stable.k8s.psg.io/kcm.provider: http
  labels:
    stable.k8s.psg.io/kcm.class: "default"
spec:
  tls:
  - hosts:
    {{if eq .KUBE_ENVIRONMENT "hof-form-example"}}
    - file-vault.prod.hof-form-example.homeoffice.gov.uk
    {{end}}
    {{if eq .KUBE_ENVIRONMENT "hof-form-example-preprod"}}
    - file-vault.preprod.hof-form-example.homeoffice.gov.uk
    {{end}}
    {{if eq .KUBE_ENVIRONMENT "acp-notprod"}}
    - file-vault-dev.notprod.hof-form-example.homeoffice.gov.uk
    {{end}}
    secretName: file-vault-cert
  rules:
  {{if eq .KUBE_ENVIRONMENT "hof-form-example"}}
  - host: file-vault.prod.hof-form-example.homeoffice.gov.uk
  {{end}}
  {{if eq .KUBE_ENVIRONMENT "hof-form-example-preprod"}}
  - host: file-vault.preprod.hof-form-example.homeoffice.gov.uk
  {{end}}
  {{if eq .KUBE_ENVIRONMENT "acp-notprod"}}
  - host: file-vault-dev.notprod.hof-form-example.homeoffice.gov.uk
  {{end}}
    http:
      paths:
      - path: /
        backend:
          serviceName: file-vault
          servicePort: 10443
