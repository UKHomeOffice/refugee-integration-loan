apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  {{ if eq .KUBE_NAMESPACE .BRANCH_ENV }}
  name: redis-permit-access-{{ .DRONE_SOURCE_BRANCH }}
  {{ else }}
  name: redis-permit-access
  {{ end }}
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          {{ if eq .KUBE_NAMESPACE .BRANCH_ENV }}
          name: refugee-loan-{{ .DRONE_SOURCE_BRANCH }}
          {{ else }}
          name: refugee-loan
          {{ end }}
    - podSelector:
        matchLabels:
          {{ if eq .KUBE_NAMESPACE .BRANCH_ENV }}
          name: refugee-loan-{{ .DRONE_SOURCE_BRANCH }}
          {{ else }}
          name: refugee-loan
          {{ end }}
    ports:
    - port: 6379
      protocol: TCP
  podSelector:
    matchLabels:
      {{ if eq .KUBE_NAMESPACE .BRANCH_ENV }}
      name: redis-{{ .DRONE_SOURCE_BRANCH }}
      {{ else }}
      name: redis
      {{ end }}
